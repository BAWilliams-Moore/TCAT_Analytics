# ODBC DSN used by the Snowflake connection page.
# On Windows this is typically a System DSN such as Snowflake_SSO.
SNOWFLAKE_DSN=Snowflake_SSO
SNOWFLAKE_USER=

# Table names come from TCAT_CENTRAL.PUBLIC.RUNS:
# DATABASE || '.' || PROJECT || '__' || ACTIVITY || SHORT_NAME || '.S540_SEGMENTED'
